# pnpm config — see https://pnpm.io/npmrc
# Hoisted linker so Metro can resolve transitive Expo deps from apps/mobile.
# Expo's monorepo docs explicitly recommend this for RN/Metro apps under pnpm.
node-linker=hoisted
shamefully-hoist=true
