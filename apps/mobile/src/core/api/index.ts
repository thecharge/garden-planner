/** Reserved for a future HTTP base layer (fetch wrapper + auth header).
 *
 * MVP speaks only to Anthropic via `@garden/engine`'s anthropicProvider and to
 * the on-device SQLite via `@garden/memory`. Nothing in this file is used yet —
 * kept as a placeholder so the FSD layout is complete from day one.
 */
export const apiPlaceholder = "unused-in-mvp" as const;
